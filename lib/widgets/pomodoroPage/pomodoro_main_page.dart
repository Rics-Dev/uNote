import 'package:flutter/material.dart';

class PomodoroMainPage extends StatelessWidget {
  const PomodoroMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pomodoro Page'),
    );
  }
}
